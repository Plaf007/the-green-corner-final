<!DOCTYPE html>
<html>
  <head>
    <title>The Green Corner</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bree+Serif&display=swap" rel="stylesheet">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon_logo.ico">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag 'https://unpkg.com/leaflet/dist/leaflet.css' %>
    <%= javascript_include_tag 'https://unpkg.com/leaflet/dist/leaflet.js' %>
  </head>

  <body>
    <div class="body-background">
      <div class="wrapper">
        <div class="content">
          <%= render "shared/navbar" %>
          <%= render "shared/flashes" %>

            <%= yield %>

        </div>
      </div>
    </div>
    <div class="footer">
        <%= render "shared/footer" %>
    </div>
  </body>
</html>
